// ADVERSARIAL AUDIT FIXTURE (PKG-H-AUDIT), target (1).
//
// Same lie, but through the *explicit, identity-bearing* channel that the
// driver treats as strongest evidence ("the only form that carries identity").
// It names ITSELF correctly, so the identity check is satisfied. It asserts
// nothing whatsoever.
console.log("GATE-EVIDENCE harness=check-idliar.js assertions=99999");
process.exit(0);
